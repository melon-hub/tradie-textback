<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Sentry Test Instructions</title>
    <style>
        body {
            font-family: system-ui, -apple-system, sans-serif;
            max-width: 800px;
            margin: 40px auto;
            padding: 20px;
            line-height: 1.6;
        }
        .step {
            background: #f5f5f5;
            padding: 15px;
            border-radius: 8px;
            margin: 15px 0;
            border-left: 4px solid #3b82f6;
        }
        code {
            background: #e5e5e5;
            padding: 2px 6px;
            border-radius: 3px;
            font-family: 'Courier New', monospace;
        }
        .console-code {
            background: #1e1e1e;
            color: #d4d4d4;
            padding: 15px;
            border-radius: 6px;
            overflow-x: auto;
            margin: 10px 0;
        }
        button {
            background: #3b82f6;
            color: white;
            border: none;
            padding: 10px 20px;
            border-radius: 6px;
            cursor: pointer;
            font-size: 16px;
            margin: 5px;
        }
        button:hover {
            background: #2563eb;
        }
        .warning {
            background: #fef3c7;
            border-left-color: #f59e0b;
        }
        .success {
            background: #d1fae5;
            border-left-color: #10b981;
        }
    </style>
</head>
<body>
    <h1>🧪 Sentry Integration Test</h1>

    <div class="step">
        <h2>Step 1: Open Your App</h2>
        <p>Make sure your dev server is running (it is!) and open:</p>
        <button onclick="window.open('http://localhost:8080', '_blank')">
            Open TradieText App
        </button>
    </div>

    <div class="step">
        <h2>Step 2: Test Sentry (Development Mode)</h2>
        <p>Go to the Sentry test page:</p>
        <button onclick="window.open('http://localhost:8080/sentry-test', '_blank')">
            Open Sentry Test Page
        </button>
        <p>This page has various test buttons to trigger different types of errors.</p>
    </div>

    <div class="step warning">
        <h2>Step 3: Quick Console Test</h2>
        <p>Open browser DevTools (F12 or Cmd+Option+I) and run in the console:</p>
        <div class="console-code">
// Test if Sentry is loaded
window.__SENTRY__ ? 'Sentry is loaded!' : 'Sentry not found'

// Send a test message
throw new Error('Test error from console - ' + new Date().toISOString())
        </div>
    </div>

    <div class="step">
        <h2>Step 4: Check Sentry Dashboard</h2>
        <p>After triggering errors, check your Sentry dashboard:</p>
        <button onclick="window.open('https://sentry.io', '_blank')">
            Open Sentry Dashboard
        </button>
        <p>Look for:</p>
        <ul>
            <li>New issues in the Issues tab</li>
            <li>Error details with stack traces</li>
            <li>User context (if logged in)</li>
            <li>Browser and device information</li>
        </ul>
    </div>

    <div class="step success">
        <h2>What to Expect</h2>
        <ul>
            <li>✅ Errors appear in Sentry within seconds</li>
            <li>✅ Stack traces show exact line numbers</li>
            <li>✅ Custom context appears in error details</li>
            <li>✅ Session replays for errors (if triggered)</li>
            <li>✅ Email notifications (if configured)</li>
        </ul>
    </div>

    <div class="step">
        <h2>Production Testing</h2>
        <p>When deployed to production:</p>
        <ul>
            <li>Sentry will automatically capture all errors</li>
            <li>DevDrawer and test page won't be available (dev-only)</li>
            <li>Real user sessions will be monitored</li>
            <li>You'll get alerts for production issues</li>
        </ul>
    </div>

    <script>
        console.log('%c🧪 Sentry Test Helper Loaded', 'color: #3b82f6; font-size: 16px; font-weight: bold');
        console.log('Follow the instructions above to test Sentry integration');
    </script>
</body>
</html>