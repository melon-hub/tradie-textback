# Example environment variables for the test script
# Copy this file to .env and fill in your actual values

# Supabase configuration
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key-here

# Note: The service role key can be found in your Supabase project:
# 1. Go to your Supabase project dashboard
# 2. Navigate to Settings > API
# 3. Find the Service Role Key under the "API keys" section
# 4. Copy the key and paste it here

# Development Tools Configuration
# Enable full dev tools in development (all features)
VITE_DEV_TOOLS=true

# Enable demo mode for safe demos/presentations (restricted features)
# Set to true when doing demos to hide dangerous actions
VITE_DEMO_TOOLS=false

# Security Note: Never commit your actual .env file to version control!
# The .env file is already in .gitignore for security.
# Both VITE_DEV_TOOLS and VITE_DEMO_TOOLS should be false in production!
