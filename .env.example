# Example environment variables
# Copy this file to .env.local for local development
# Add these to Netlify dashboard for production deployment

# Supabase configuration (Required for Netlify)
VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key-here

# For local development scripts only (not needed in Netlify)
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key-here

# Note: The service role key can be found in your Supabase project:
# 1. Go to your Supabase project dashboard
# 2. Navigate to Settings > API
# 3. Find the Service Role Key under the "API keys" section
# 4. Copy the key and paste it here

# Development Tools Configuration
# Enable full dev tools in development (all features)
VITE_DEV_TOOLS=true

# Enable demo mode for safe demos/presentations (restricted features)
# Set to true when doing demos to hide dangerous actions
VITE_DEMO_TOOLS=false

# Google Maps API Key (for service area visualization)
VITE_GOOGLE_MAPS_API_KEY=your-google-maps-api-key-here

# Sentry Error Monitoring (Optional but recommended for production)
# Get your DSN from https://sentry.io after creating a project
VITE_SENTRY_DSN=https://YOUR_DSN@sentry.io/YOUR_PROJECT_ID

# Sentry Auth Token for uploading source maps (production builds only)
# Get from: https://sentry.io/settings/account/api/auth-tokens/
# Required scopes: project:releases, org:read
SENTRY_AUTH_TOKEN=your-sentry-auth-token-here

# Security Note: Never commit your actual .env file to version control!
# The .env file is already in .gitignore for security.
# Both VITE_DEV_TOOLS and VITE_DEMO_TOOLS should be false in production!
